# Frontend React

App React + TypeScript minimal per requisiti: Home, Wishlist detail, Add/Remove, Header, Empty/Skeleton, theming, accessibility.

Run:

```bash
cd frontend-react
npm install
npm run dev
```
