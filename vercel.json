{
  "version": 2,
  "buildCommand": "npm --prefix backend run db:generate && npm --prefix backend run build",
  "builds": [
    { "src": "api/index.ts", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.ts" }
  ]
}
